<?xml version="1.0"?>
<root main_tree_to_execute="BehaviorTree">
    <!-- ////////// -->
    <BehaviorTree ID="BehaviorTree">
        <KeepRunningUntilFailure>
            <Sequence>
                <Action ID="GetNextWp" waypoint="{wp}"/>
                <ForceSuccess>
                    <Timeout msec="10000">
                        <Action ID="Move" goal="{wp}"/>
                    </Timeout>
                </ForceSuccess>
            </Sequence>
        </KeepRunningUntilFailure>
    </BehaviorTree>
    <!-- ////////// -->
    <TreeNodesModel>
        <Action ID="GetNextWp">
            <output_port name="waypoint"/>
        </Action>
        <Action ID="Move">
            <input_port name="goal"/>
        </Action>
    </TreeNodesModel>
    <!-- ////////// -->
</root>

